export const writeToDB = ( appointmentData ) => {
   //POST request 
   console.log(appointmentData);
}

export const deleteFromDB = ( appointmentData ) => {
   //DELETE request
   console.log(appointmentData);
}

export const updateDB =  (updatedData ) => {
   //PUT request
   console.log (updatedData);
}